The webpage works offline, however for security reasons the webpage will
not load the database files if the page is not being hosted on a server.

The main webpage is 'twarchive.html'
'twarchive_date.html' was made as a test for header spacing etc.

I showed that the page works and properly loads during the final critique.
